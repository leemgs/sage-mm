# JSA submission checklist — SAGE-MM

Target venue: **Journal of Systems Architecture** (Elsevier, `elsarticle`
class, submitted through Editorial Manager). Publish under the **subscription**
route (no APC); do **not** select Gold Open Access.

This checklist is the single source of truth for what is prepared and what the
author must still do. Nothing here fabricates a result: the manuscript compiles
as an internal, watermarked draft, and the fail-closed gate
(`scripts/check_jsa_readiness.py`) refuses to call it submission-ready until the
real evaluation exists.

---

## 1. Files in this repository, and where each goes in Editorial Manager

| Repo file | EM item type | Status |
|---|---|---|
| `paper/sagemm-jsa.tex` | Manuscript (LaTeX source) | **Draft** — compiles, but in simulation mode (see §3) |
| `paper/sagemm.bib` | Bibliography (uploaded with source) | **Blocked** — verify DOIs (see §3) |
| `paper/generated/observed-results.tex`, `paper/generated/simulated-results.tex` | Uploaded with source (`\input`) | Ready (proxy/simulation, watermarked) |
| `paper/docs/figures/*.svg` | Convert to PDF/EPS before upload | Ready as placeholders |
| `paper/HIGHLIGHTS_JSA.md` / `.tex` | **Highlights** (separate file) | Ready |
| `paper/COVER_LETTER_JSA.md` | **Cover letter** | Ready — fill `[Date]`, `[Institution]`, optional reviewers |
| `paper/DECLARATIONS_JSA.md` | Pasted into EM declaration fields | Ready (single author, none declared) |

The competing-interest, funding, data-availability, and CRediT statements are
also embedded in the compiled manuscript (generated by `scripts/make_jsa.py`),
so the PDF and the EM form fields agree.

## 2. What is DONE

- [x] Manuscript retargeted to JSA (elsarticle front/back matter) and kept in
      sync with the TECS source via `scripts/make_jsa.py`.
- [x] CRediT, Declaration of competing interest (none), Funding (none), and
      Data availability statements in both the manuscript and `DECLARATIONS_JSA.md`.
- [x] Highlights written and verified ≤ 85 characters each (5 items).
- [x] Cover letter updated to single author with no competing interests / no funding.
- [x] Author affiliation filled: Geunsik Lim, Sungkyunkwan University, Suwon,
      Republic of Korea (front matter + cover letter).
- [x] Bibliography DOIs audited against the publisher of record (see §3.3 and
      `docs/LITERATURE_AUDIT.md`); the readiness gate's bibliography blocker is
      cleared. **Only blocker #1 (real firmware Results) now gates submission.**
- [x] Observed proxy-harness bundle integrated with honest provenance and CIs.
- [x] JSA build script (`scripts/build_jsa.sh`) and fail-closed readiness gate
      (`scripts/check_jsa_readiness.py`) added.
- [x] Appendix de-ACM'd (venue-neutral "artifact evaluation").

## 3. BLOCKERS the author must clear before clicking Submit

These are reported live by `python3 scripts/check_jsa_readiness.py`:

1. **Real Results.** The manuscript is built with `\includesimulationtrue`
   (preregistered hypotheses + simulation dry-run + proxy-harness table). A
   submission needs vendor DTV Mono/.NET6 device measurements: run the
   preregistered full-factorial G×I×R×C campaign (≥30 cold-reset runs/cell,
   EventPipe/vendor-GC trace, per-mapping `Private_Clean`, ≥30 min and 8 h
   durations), regenerate `paper/generated/observed-results.tex` from that
   bundle, then set `\includesimulationfalse` in `paper/sagemm-tecs.tex` and
   rerun `scripts/make_jsa.py`. The current `observed-results.tex` is the ARM64
   Linux **proxy harness** (n=3, Python collector) and is explicitly *not*
   firmware evidence.
2. ~~**Author affiliation.**~~ Done: Geunsik Lim, Sungkyunkwan University,
   Suwon, Republic of Korea (in `scripts/make_jsa.py` preamble and cover letter).
3. ~~**Bibliography verification.**~~ Done: DOIs audited against the publisher
   of record. DumpKV corrected to DOI `10.14778/3717755.3717778` with the right
   co-authors; LXR DOI confirmed; Platinum given the full author list and its
   USENIX URL (no DOI exists); AGC cited by its IEEE Xplore record (article
   11298441) — its IEEE DOI could not be confirmed from a primary source in
   this environment, so take the final volume/issue/pages/DOI from that Xplore
   page before submission. Optionally still expand the related-work set beyond
   these five references.

## 4. Recommended before submission (not gated)

- Convert the three placeholder SVG figures and the architecture figure to
  vector PDF/EPS; confirm they are legible in single-column width.
- Fill the "Use of generative AI" disclosure in `DECLARATIONS_JSA.md` to match
  the actual process, or delete it.
- Add 3 suggested reviewers (no conflict of interest) in the cover letter / EM.
- Confirm current JSA author instructions: page/length policy, figure formats,
  reference style, and the subscription-vs-OA publishing-option step.
- Run `bash scripts/build_jsa.sh` locally with a TeX Live that has
  `elsarticle.cls` to resolve any overfull-box / citation warnings.

## 5. Final gate

From the repository root:

```bash
bash scripts/build_jsa.sh --submission   # fails until §3 is cleared, by design
```

When it prints `JSA submission readiness gate passed` and produces
`paper/sagemm-jsa.pdf`, the manuscript, highlights, cover letter, and
declarations are ready to upload through Editorial Manager under the
subscription route.
